/**
 * Ogni prenotazione avrà un id diverso, ma conterrà id di worker e owner.
 * Gli stati di una prenotazione possono essere:
 * - waiting, prenotazione inserita dall'owner
 * - approved, prenotazione approvata dallo user
 * - valutata, prenotazione approvata dallo user e valutata dall'owner
 * - refused, prenotazione rifiutata dallo user
 *
 **/
/*
enum ReservationStatus{
    waiting,
    approved,
    rejected,
    evaluated
}

module.exports = ReservationStatus;*/